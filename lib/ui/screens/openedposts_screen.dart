import 'package:flutter/material.dart';

class OpenedPostsScreen extends StatelessWidget {
  const OpenedPostsScreen();

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
